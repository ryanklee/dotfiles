# GuC (GuC command submission) + HuC (firmware authentication).
# enable_guc=3 = both GuC submission and HuC loading.
# Benefits: HW video decode via HuC, better iGPU power management.
# Takes effect on next reboot (or i915 module reload, which restarts the desktop).
options i915 enable_guc=3
