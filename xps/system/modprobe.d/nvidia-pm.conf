# Turing+ dynamic power management (RTD3) — GTX 1650 Ti is Turing.
# 0x02 = fine-grained power management (per-engine sleep).
# Allows the GPU to drop to D3cold (essentially off) when idle.
# Takes effect on next nvidia module reload or reboot.
options nvidia NVreg_DynamicPowerManagement=0x02
