# Hypridle — ALL idle actions disabled
# No lock, no DPMS, no power saving. Everything always on.
# Manual lock only: Super+Shift+Escape
general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}
