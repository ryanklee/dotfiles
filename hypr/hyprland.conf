
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
# Match by description so connector names (DP-1/DP-2/DP-4/DP-5) don't matter.
# PCIe reenumeration (e.g. adding a second GPU) renames connectors and broke
# the old DP-1/DP-2 matching. Description uses make + model + serial and is
# stable across hardware moves.
# Primary (left): Dell S2721DGF — 27" 2560x1440 FreeSync Premium Pro, 165Hz max.
# Running fixed 165Hz with VRR off + direct_scanout off (see render{} block) —
# NVIDIA open driver DMA-buf bugs cause blinks during mode transitions.
# cm,srgb explicitly locks the color path (DO NOT enable cm,hdr — HDR-exit
# brightness bug unresolved on this 8-bit-FRC IPS panel).
monitor = desc:Dell Inc. DELL S2721DGF 7MJQX83, 2560x1440@165, 0x0, 1, cm, srgb
# Secondary (right): Dell U2412M — 24" 1920x1200 60Hz IPS — Logos monitor
# y=240 bottom-aligns with the taller primary monitor (1440-1200=240)
monitor = desc:Dell Inc. DELL U2412M YMYH12BR44TL, 1920x1200@60, 2560x240, 1, cm, srgb


###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = foot
$fileManager = yazi
$menu = fuzzel
$logos = hapax-logos-app


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# Crash-report dir + theme cache. Hyprland writes ~/.cache/hyprland/hyprlandCrashReport*.txt
# and hapax-theme-apply stages active config artifacts under ~/.cache/hapax/theme/.
exec-once = mkdir -p ~/.cache/hyprland ~/.cache/hapax/theme

# Set R&D mode + theme BEFORE waybar/mako so they have correct state on first render
exec-once = mkdir -p ~/.cache/hapax && echo rnd > ~/.cache/hapax/working-mode && ~/.local/bin/hapax-theme-apply rnd

# Bootstrap the systemd user session: import Wayland env and activate
# hyprland-session.target, which BindsTo graphical-session.target. Without
# this, waybar (and any other graphical-session.target-gated unit) won't
# start at login because graphical-session.target refuses manual start and
# has no other unit pulling it active.
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY DISPLAY XDG_CURRENT_DESKTOP HYPRLAND_INSTANCE_SIGNATURE; systemctl --user import-environment WAYLAND_DISPLAY DISPLAY XDG_CURRENT_DESKTOP HYPRLAND_INSTANCE_SIGNATURE; systemctl --user start hyprland-session.target

exec-once = systemctl --user start waybar.service
# mako via systemd: gets restart-on-failure, journald logs, env import.
exec-once = systemctl --user start mako.service
exec-once = solaar --window=hide
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# cliphist: 500-item cap (was unlimited dedupe; default 750 cap). Trims DB pressure.
exec-once = wl-paste --watch cliphist store --max-items 500 --max-dedupe-search 500
# hypridle disabled. Studio compositor + livestream require 24/7 wake state.
# Manual lock only: Super+Shift+Escape. To opt into idle-lock for multi-day vacation:
# `systemctl --user enable --now hypridle.service` with a fortress-aware listener
# config; current hypridle.conf has stub commands but no listeners (intentional).
# exec-once = hypridle
exec-once = hyprpaper
exec-once = ~/.local/bin/hypr-scripts/singleton-guard.sh
exec-once = ~/.local/bin/hypr-scripts/default-workspaces.sh
# hyprsunset is started by hapax-theme-apply with the per-mode HYPRSUNSET_TEMP_K
# (rnd=3200K warm, research=4500K neutral). Single source of truth — do not
# also start it here, or the two would fight.
exec-once = qs-overview start
# KDE Connect tray indicator (phone↔workstation file/clipboard sync via hapax-phone)
exec-once = kdeconnect-indicator


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

env = XCURSOR_SIZE,32
env = XCURSOR_THEME,Bibata-Modern-Classic
env = HYPRCURSOR_THEME,Bibata-Modern-Classic
env = HYPRCURSOR_SIZE,32

# NVIDIA — required for hardware-accelerated Wayland rendering
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = NVD_BACKEND,direct
# GPU mapping (verified 2026-05-01 via /dev/dri/by-path):
#   pci-0000:01:00.0-card  → 5060 Ti  (display GPU, cable here as of 2026-04-14)
#   pci-0000:05:00.0-card  → 3090     (compute, no display attached)
#   pci-0000:73:00.0-card  → AMD iGPU
#
# AQ_DRM_DEVICES is exported by ~/.local/bin/start-hyprland-wrapper before
# Hyprland starts, using the stable PCI by-path symlink — cardN numbering can
# change across kernel updates. Aquamarine doesn't follow symlinks in this env
# var (Hyprland #6997), so the wrapper resolves it to the live cardN value.
# The line below remains as a redundant fallback in case the wrapper is
# bypassed; the wrapper writes the same value on a working system today.
env = AQ_DRM_DEVICES,/dev/dri/card1
# Multi-GPU NVIDIA hardening: avoid forcing linear modifiers on cross-GPU buffers.
env = AQ_FORCE_LINEAR_BLIT,0
env = QT_QPA_PLATFORM,wayland
env = MOZ_ENABLE_WAYLAND,1

# Qt/GTK dark theme
env = QT_QPA_PLATFORMTHEME,qt6ct
env = GTK_THEME,gruvbox-dark-gtk
env = GTK_CSD,0


###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

permission = /usr/(bin|local/bin)/grim, screencopy, allow
permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
permission = /usr/(bin|local/bin)/slurp, screencopy, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 4

    border_size = 2

    # Gruvbox R&D default — overridden at runtime by hapax-theme-apply
    col.active_border = rgb(fabd2f)
    col.inactive_border = rgb(504945)

    resize_on_border = true
    allow_tearing = true
    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 8
    rounding_power = 2.0

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.92

    dim_inactive = true
    dim_strength = 0.12

    shadow {
        enabled = true
        range = 12
        render_power = 3
        color = rgba(1a1a1aee)
        color_inactive = rgba(1a1a1a88)
    }

    # Disabled — saves GPU for compute (Ollama, faster-whisper, etc.)
    blur {
        enabled = false
    }
}

# Window opacity — active inactive
windowrule {
    name = opacity-obsidian
    match:class = obsidian
    opacity = 0.95 0.90
}
windowrule {
    name = opacity-vscode
    match:class = code-url-handler
    opacity = 0.98 0.92
}
windowrule {
    name = opacity-firefox
    match:class = firefox
    opacity = 1.0 0.95
}

# Tearing for fullscreen games
windowrule {
    name = tear-steam
    match:class = steam_app_.*
    immediate = true
}
windowrule {
    name = tear-gamescope
    match:class = gamescope
    immediate = true
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # Snappy ~200ms animations; refresh-rate-independent (SPEED is in time units, not frames)
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     5,     easeOutQuint
    animation = border,        1,     3,     easeOutQuint
    animation = windows,       1,     3,     easeOutQuint
    animation = windowsIn,     1,     2.5,   easeOutQuint, popin 80%
    animation = windowsOut,    1,     1,     linear,       popin 80%
    animation = fadeIn,        1,     1.2,   almostLinear
    animation = fadeOut,       1,     1,     almostLinear
    animation = fade,          1,     2,     quick
    animation = layers,        1,     2.5,   easeOutQuint
    animation = layersIn,      1,     2.5,   easeOutQuint, fade
    animation = layersOut,     1,     1,     linear,       fade
    animation = fadeLayersIn,  1,     1.2,   almostLinear
    animation = fadeLayersOut, 1,     1,     almostLinear
    animation = workspaces,    1,     1.5,   almostLinear, fade
    animation = workspacesIn,  1,     1,     almostLinear, fade
    animation = workspacesOut, 1,     1.5,   almostLinear, fade
}


# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true
    preserve_split = true
    # Bias new splits toward the active window's geometry rather than cardinal
    # direction — placement feels "where I am" rather than purely geometric.
    split_bias = 1
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true
    background_color = rgb(0a0a0a)
    # Focus new windows that request it
    focus_on_activate = true
    # VRR/FreeSync disabled — causes blink clusters on NVIDIA open driver.
    # Fixed 120Hz is stable and sufficient for dev work.
    vrr = 0
}

cursor {
    # Software cursors on NVIDIA Wayland eliminate an entire bug class:
    # 580.x ghost-cursor on shape change, multi-monitor cursor lag in
    # GPU-intensive apps, etc. ~6ms theoretical SW-cursor latency at 165Hz —
    # imperceptible. Reversible: flip to false if a real NVIDIA fix lands.
    no_hardware_cursors = true
}

render {
    # Disabled — causes blinks on NVIDIA open driver during scanout mode transitions
    # (fullscreen enter/exit, workspace population at login)
    direct_scanout = false
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:ctrl_modifier,compose:ralt
    kb_rules =

    follow_mouse = 1
    sensitivity = 0
    accel_profile = flat

    repeat_rate = 60
    repeat_delay = 250
    numlock_by_default = true

    touchpad {
        natural_scroll = false
    }
}

# MX Ergo S trackball (via Bolt receiver)
device {
    name = logitech-usb-receiver-mouse
    accel_profile = flat
    sensitivity = 0
    scroll_factor = 1.5
}

# MX Ergo S trackball (via direct Bluetooth) — verify with `hyprctl devices` when BT-connected
device {
    name = mx-ergo-s
    accel_profile = flat
    sensitivity = 0
    scroll_factor = 1.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# ── Core ──────────────────────────────────────────────────────────────────────
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Q, exec, $logos
bind = $mainMod, C, killactive,
bind = $mainMod, E, exec, foot -e yazi
bind = $mainMod, R, exec, $menu
bind = $mainMod, X, exec, wlogout
bind = $mainMod SHIFT, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod SHIFT, C, exec, hyprctl reload
bind = $mainMod, M, exec, ~/.local/bin/hapax-working-mode $([ $(~/.local/bin/hapax-working-mode) = rnd ] && echo research || echo rnd)

# ── Windows ───────────────────────────────────────────────────────────────────
bind = $mainMod, F, fullscreen, 1
bind = $mainMod SHIFT, F, fullscreen, 0
bind = $mainMod SHIFT, Space, togglefloating,
bind = $mainMod, A, focusurgentorlast,

# Window grouping (tabs)
bind = $mainMod, G, togglegroup,
bind = $mainMod, TAB, changegroupactive, f
bind = $mainMod SHIFT, TAB, changegroupactive, b

# ── Focus — vim keys + arrows ────────────────────────────────────────────────
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# ── Move windows — vim keys + arrows ─────────────────────────────────────────
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d

# ── Screenshots (unified screenshot.sh) ──────────────────────────────────────
# screenshot.sh wraps grim+slurp+satty with `timeout 10`, atomic write (.tmp+mv),
# 0-byte capture detection (silent screencopy failure), and consistent notify-send.
bind = , Print,             exec, ~/.local/bin/hypr-scripts/screenshot.sh full
bind = $mainMod, Print,     exec, ~/.local/bin/hypr-scripts/screenshot.sh region
bind = $mainMod SHIFT, Print, exec, ~/.local/bin/hypr-scripts/screenshot.sh annotate
bind = $mainMod ALT, Print, exec, ~/.local/bin/hypr-scripts/screenshot.sh window

# ── Screen recording (wf-recorder) ───────────────────────────────────────────
bind = $mainMod, W, exec, ~/.local/bin/hypr-scripts/screen-record-toggle.sh
bind = $mainMod SHIFT, W, exec, ~/.local/bin/hypr-scripts/screen-record-toggle.sh region

# ── Clipboard ─────────────────────────────────────────────────────────────────
# SUPER+Y: pick from history via fuzzel, then auto-paste the selection
bind = $mainMod, Y, exec, cliphist list | fuzzel -d | cliphist decode | wl-copy && wtype -M ctrl -k v -m ctrl
# SUPER+SHIFT+Y: wipe clipboard history (privacy / cleanup)
bind = $mainMod SHIFT, Y, exec, cliphist wipe && notify-send "Clipboard history wiped"

# ── Notifications (mako) ─────────────────────────────────────────────────────
bind = $mainMod, D, exec, makoctl mode -t do-not-disturb
bind = $mainMod, O, exec, makoctl invoke
bind = $mainMod SHIFT, O, exec, makoctl dismiss -a

# ── Tools ─────────────────────────────────────────────────────────────────────
bind = $mainMod, P, exec, hyprpicker -a
bind = $mainMod, B, exec, killall -SIGUSR1 waybar
# Bluetooth GUI (overskride — Wayland-native; blueman-tray is broken on Wayland)
bind = $mainMod SHIFT, B, exec, overskride

# ── Special workspaces (music / chat / keepass / bt) ──────────────────────────
bind = $mainMod, F2, togglespecialworkspace, music
bind = $mainMod, F3, togglespecialworkspace, chat
bind = $mainMod, F4, togglespecialworkspace, keepass

# ── System ────────────────────────────────────────────────────────────────────
bind = $mainMod SHIFT, Escape, exec, hyprlock

# Hotkey cheatsheet popup (toggle — kills existing or spawns new)
bind = $mainMod, F1, exec, hyprctl clients -j | grep -q '"Hotkey Cheatsheet"' && hyprctl dispatch closewindow title:^Hotkey Cheatsheet$ || foot --title="Hotkey Cheatsheet" --override=pad=20x10 ~/.local/bin/hypr-scripts/hotkey-cheatsheet.sh

# ── Scratchpads ───────────────────────────────────────────────────────────────
bind = $mainMod, grave, exec, ~/.local/bin/hypr-scripts/llm-scratchpad.sh
bind = $mainMod SHIFT, grave, movetoworkspace, special:llm
bind = $mainMod, S, exec, ~/.local/bin/hypr-scripts/scratchpad.sh
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod, N, exec, ~/.local/bin/hypr-scripts/notepad-scratchpad.sh
bind = $mainMod SHIFT, N, movetoworkspace, special:notepad

# ── Workspaces (monitor-aware) ────────────────────────────────────────────────
# SUPER+N switches to WS N on primary or WS N+10 on secondary
bind = $mainMod, 1, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 1
bind = $mainMod, 2, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 2
bind = $mainMod, 3, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 3
bind = $mainMod, 4, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 4
bind = $mainMod, 5, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 5
bind = $mainMod, 6, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 6
bind = $mainMod, 7, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 7
bind = $mainMod, 8, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 8
bind = $mainMod, 9, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 9
bind = $mainMod, 0, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh switch 9

bind = $mainMod SHIFT, 1, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 1
bind = $mainMod SHIFT, 2, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 2
bind = $mainMod SHIFT, 3, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 3
bind = $mainMod SHIFT, 4, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 4
bind = $mainMod SHIFT, 5, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 5
bind = $mainMod SHIFT, 6, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 6
bind = $mainMod SHIFT, 7, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 7
bind = $mainMod SHIFT, 8, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 8
bind = $mainMod SHIFT, 9, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 9
bind = $mainMod SHIFT, 0, exec, ~/.local/bin/hypr-scripts/workspace-switch.sh move 9

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# ── Multi-monitor ────────────────────────────────────────────────────────
bind = $mainMod, comma, focusmonitor, l
bind = $mainMod, period, focusmonitor, r
bind = $mainMod SHIFT, comma, movewindow, mon:l
bind = $mainMod SHIFT, period, movewindow, mon:r
bind = $mainMod ALT, comma, swapactiveworkspaces, DP-2 DP-1

# ── Mouse ─────────────────────────────────────────────────────────────────────
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Thumb wheel gestures — flick left = copy, flick right = paste
bind = , mouse_left, exec, wl-paste -p 2>/dev/null | wl-copy
bind = , mouse_right, exec, wl-paste -n | wtype -

# ── Media ─────────────────────────────────────────────────────────────────────
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# ── Voice daemon ──────────────────────────────────────────────────────────────
# Push-to-talk (hold Super+V to talk, release to stop)
bindr = $mainMod, V, exec, ~/.local/bin/hypr-scripts/voice-ptt.sh release
bind = $mainMod, V, exec, ~/.local/bin/hypr-scripts/voice-ptt.sh press
# Agent invocation
bind = $mainMod SHIFT, V, exec, ~/.local/bin/hypr-scripts/voice-invoke.sh
# Voice daemon toggle (process-compose managed, not systemd — toggle via socket)
bind = $mainMod ALT, V, exec, hapax-toggle

# ── Resize submap ─────────────────────────────────────────────────────────────
bind = $mainMod ALT, R, submap, resize
submap = resize
binde = , H, resizeactive, -30 0
binde = , L, resizeactive, 30 0
binde = , K, resizeactive, 0 -30
binde = , J, resizeactive, 0 30
binde = , left, resizeactive, -30 0
binde = , right, resizeactive, 30 0
binde = , up, resizeactive, 0 -30
binde = , down, resizeactive, 0 30
bind = , Escape, submap, reset
bind = , Return, submap, reset
submap = reset


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run
    match:class = hyprland-run
    move = 20 monitor_h-120
    float = yes
}

# Hotkey cheatsheet — centered float
windowrule {
    name = cheatsheet-float
    match:title = ^Hotkey Cheatsheet$
    float = yes
    size = 560 920
    center = true
}

# LLM scratchpad — left side, beside notepad
windowrule {
    name = llm-scratchpad
    match:workspace = special:llm
    float = yes
    size = 770 690
    move = 4 28
    opacity = 1.0 1.0
    border_size = 3
}

# General scratchpad — right side, beside notepad
windowrule {
    name = general-scratchpad
    match:workspace = special:magic
    float = yes
    size = 770 690
    move = 1786 28
    opacity = 1.0 1.0
    border_size = 3
}

# Notepad scratchpad — top-center, below waybar to mid-screen
windowrule {
    name = notepad-scratchpad
    match:workspace = special:notepad
    float = yes
    size = 1000 690
    move = 780 28
    opacity = 1.0 1.0
    border_size = 3
}
# ── Workspace → monitor bindings ─────────────────────────────────────────
# WS 1-9 on primary (DP-1, Dell S2721DGF 2560x1440), WS 11-19 on secondary (DP-2, Dell U2412M 1920x1200).
# Both monitors get independent 1-9 switching via workspace-switch.sh.
workspace = 1, monitor:DP-1, default:true
workspace = 2, monitor:DP-1
workspace = 3, monitor:DP-1
workspace = 4, monitor:DP-1
workspace = 5, monitor:DP-1
workspace = 6, monitor:DP-1
workspace = 7, monitor:DP-1
workspace = 8, monitor:DP-1
workspace = 9, monitor:DP-1
workspace = 11, monitor:DP-2, default:true
workspace = 12, monitor:DP-2
workspace = 13, monitor:DP-2
workspace = 14, monitor:DP-2
workspace = 15, monitor:DP-2
workspace = 16, monitor:DP-2
workspace = 17, monitor:DP-2
workspace = 18, monitor:DP-2
workspace = 19, monitor:DP-2

# Single-window workspaces: remove gaps (replaces removed no_gaps_when_only).
# w[t1] = single tiled window only — preserves gaps when a float (e.g. scratchpad) is also present.
workspace = w[t1], gapsout:0, gapsin:0

# ── Logos window rules ───────────────────────────────────────────────────
# Hapax Logos Chrome app → always on WS 10 (secondary/right monitor), fullscreen
windowrule {
    name = logos-to-right-monitor
    match:class = ^chrome-localhost__terrain-Default$
    workspace = 11
    fullscreen = 1
}

# ── Floating rules for dialogs and system windows ─────────────────────
windowrule {
    name = float-file-dialogs
    match:title = ^(Open|Save|Save As|File Upload|Choose Files|Select a File|Select Folder)
    float = yes
    center = true
    size = 960 640
}

windowrule {
    name = float-auth-dialogs
    match:class = ^(polkit-gnome-authentication-agent-1|gcr-prompter|pinentry-.*)$
    float = yes
    center = true
}

windowrule {
    name = float-portal-dialogs
    match:class = ^(xdg-desktop-portal-gtk)$
    float = yes
    center = true
}

# ── Picture-in-Picture ────────────────────────────────────────────────
windowrule {
    name = pip-float
    match:title = ^(Picture.in.[Pp]icture)$
    float = yes
    pin = true
    keep_aspect_ratio = true
    size = 480 270
    move = 100% -280
}

# ── Group/tab styling (Gruvbox R&D default; overridden at runtime by hapax-theme-apply) ─────
group {
    col.border_active = rgb(fabd2f)
    col.border_inactive = rgb(504945)
    groupbar {
        font_size = 11
        text_padding = 8
        height = 24
        col.active = rgb(fabd2f)
        col.inactive = rgb(282828)
        text_color = rgb(ebdbb2)
    }
}

# ── App workspace pinning ─────────────────────────────────────────────
windowrule {
    name = chrome-to-ws3
    match:class = ^(google-chrome)$
    workspace = 3
}

windowrule {
    # OBS lives on DP-2 ws11 alongside Logos in actual workflow (was incorrectly ws5).
    name = obs-to-ws11
    match:class = ^(com.obsproject.Studio)$
    workspace = 11
}

windowrule {
    name = keep-logos-opaque
    match:class = ^chrome-localhost__terrain-Default$
    opacity = 1.0 1.0
}


windowrule {
    name = keep-foot-opaque
    match:class = ^(foot)$
    opacity = 1.0 1.0
}


# ── JetBrains IDE focus-stealing fixes ──
# DEFERRED: the upstream-recommended rule pack uses match:floating / match:tag /
# tag=+ / stayfocused / noinitialfocus, which Hyprland 0.54.3 rejects in v2 block
# syntax (verified live 2026-05-01). Re-add when 0.55+ ships or if the operator
# starts using a JetBrains IDE — at that point port to v1 windowrulev2 = ...
# syntax (where these keywords are confirmed to work).

# ── Cursor IDE / Code derivatives (extend existing vscode opacity) ──────────
windowrule {
    name = opacity-cursor
    match:class = ^(Cursor|cursor-url-handler)$
    opacity = 0.98 0.92
}

# ── Secret managers + KeePassXC ─────────────────────────────────────────────
windowrule {
    name = keepassxc-main
    match:class = ^(org\.keepassxc\.KeePassXC)$
    float = yes
    size = 1000 720
    center = true
}
windowrule {
    name = keepassxc-unlock
    match:class = ^(org\.keepassxc\.KeePassXC)$
    match:title = ^(Unlock Database.*)$
    float = yes
    center = true
    pin = true
}
windowrule {
    name = vault-popup
    match:class = ^(Bitwarden|1Password|com\.bitwarden\.desktop)$
    float = yes
    center = true
    size = 540 720
}

# ── Messaging apps ──────────────────────────────────────────────────────────
windowrule {
    name = discord-popouts
    match:class = ^(discord|vesktop|WebCord)$
    match:title = .*(Call|Screen Share|Pop ?Out).*
    float = yes
    size = 800 600
    center = true
}
windowrule {
    name = slack-huddle
    match:class = ^(Slack)$
    match:title = ^(Huddle.*|Slack \| mini panel.*)$
    float = yes
}
windowrule {
    name = signal-popups
    match:class = ^(signal)$
    match:title = .*(Link Preview|About).*
    float = yes
    center = true
}
windowrule {
    name = telegram-media
    match:class = ^(org\.telegram\.desktop|telegram-desktop)$
    match:title = ^(Media viewer)$
    float = yes
    size = 1280 720
    center = true
}

# ── Image viewers + media + calculator ──────────────────────────────────────
windowrule {
    name = image-viewers
    match:class = ^(imv|qView|swayimg|eog|org\.gnome\.eog)$
    float = yes
    center = true
    size = 1280 800
}
windowrule {
    name = mpv-pip-pin
    match:class = ^(mpv|smplayer|vlc)$
    match:float = true
    pin = true
    keep_aspect_ratio = true
}
windowrule {
    name = calculator
    match:class = ^(org\.gnome\.Calculator|gnome-calculator|qalculate-gtk)$
    float = yes
    center = true
    size = 420 540
}

# ── OBS source-preview / projector / settings windows ───────────────────────
windowrule {
    name = obs-projector
    match:class = ^(com\.obsproject\.Studio)$
    match:title = ^(Windowed Projector.*|Fullscreen Projector.*|Source Preview.*|Properties for.*|Settings.*)$
    float = yes
    center = true
}

# ── Hapax surface pinning ───────────────────────────────────────────────────
windowrule {
    name = hapax-imagination-to-ws12
    match:class = ^(hapax-imagination|hapax-imagination-loop)$
    workspace = 12
    fullscreen = 1
}
windowrule {
    name = studio-compositor-to-ws11
    match:class = ^(studio-compositor|hapax-studio-compositor)$
    workspace = 11
}
windowrule {
    name = hapax-reverie-fullscreen
    match:class = ^(hapax-reverie)$
    fullscreen = 1
}

# ── Special workspace pinning (music, chat, keepass) ────────────────────────
windowrule {
    name = music-special
    match:class = ^(Spotify|spotify)$
    workspace = special:music silent
}
windowrule {
    name = chat-discord
    match:class = ^(discord|vesktop|WebCord)$
    workspace = special:chat silent
}
windowrule {
    name = chat-slack
    match:class = ^(Slack)$
    workspace = special:chat silent
}
windowrule {
    name = keepass-special
    match:class = ^(org\.keepassxc\.KeePassXC)$
    workspace = special:keepass silent
}

# ── KDE Connect main window + blueman-manager (one-shot launches) ───────────
windowrule {
    name = kdeconnect-app
    match:class = ^(org\.kde\.kdeconnect.*)$
    float = yes
    center = true
}
windowrule {
    name = blueman
    match:class = ^(blueman-manager)$
    float = yes
    center = true
    size = 720 520
}

# ── Wine installers, game launchers ─────────────────────────────────────────
windowrule {
    name = wine-installers
    match:class = ^(Wine|.*\.exe)$
    match:title = .*(Setup|Install|Wizard).*
    float = yes
    center = true
}
windowrule {
    name = game-launchers-ws8
    match:class = ^(heroic|net\.lutris\.Lutris|com\.heroicgameslauncher\.hgl)$
    workspace = 8
}
windowrule {
    name = steam-friends
    match:class = ^(steam)$
    match:title = ^(Friends List)$
    float = yes
    size = 400 720
}
windowrule {
    name = steam-splash
    match:class = ^(steam)$
    match:title = ^$
    float = yes
}

# ── Splash screens (LibreOffice, Krita, Blender) ────────────────────────────
windowrule {
    name = libreoffice-splash
    match:class = ^(soffice|LibreOffice)$
    match:title = ^$
    float = yes
}
windowrule {
    name = krita-splash
    match:class = ^(krita)$
    match:title = ^(Welcome.*|About.*)$
    float = yes
    center = true
}
windowrule {
    name = blender-splash
    match:class = ^(Blender|blender)$
    match:title = ^(Blender)$
    match:float = true
    center = true
}


xwayland {
    force_zero_scaling = true
}

# ── Plugins ──────────────────────────────────────────────────────────────────
# No plugins loaded into the running Hyprland (verify: `hyprctl plugin list`).
# `hyprpm list` shows borders-plus-plus, hyprexpo, hyprwinwrap as enabled, but
# this conf does not `plugin = …` source them, so they are not active.
# To activate hyprexpo (workspace overview), bind a key and source the plugin:
#   plugin = ~/.local/share/hyprpm/hyprland-plugins/hyprexpo/hyprexpo.so
#   bind = $mainMod, Tab, hyprexpo:expo, toggle
# Hold off until 0.55+ ships: hyprexpo crashes documented on NVIDIA (#475).
#
# Failed builds (do not retry until upstream fix): hyprscrolling, hyprtrails,
# Hyprspace. Run `hyprpm remove <name>` to stop reload churn.
